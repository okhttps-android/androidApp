<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    android:id="@+id/book_service_search_action"
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="48dp">

    <ImageView
        android:id="@+id/book_service_search_back"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_centerVertical="true"
        android:src="@drawable/ic_baseutil_back" />

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="36dp"
        android:layout_centerInParent="true"
        android:layout_marginLeft="40dp"
        android:layout_marginRight="40dp"
        android:visibility="visible">

        <com.core.widget.ClearEditText
            android:id="@+id/book_service_search_et"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:layout_centerVertical="true"
            android:background="@drawable/search_input"
            android:drawableLeft="@drawable/search"
            android:drawableRight="@drawable/search_clear"
            android:hint="请输入餐厅/会所等名称"
            android:imeOptions="actionSearch"
            android:inputType="text"
            android:paddingRight="26dp"
            android:textColor="@color/dark_grey"
            android:textColorHint="@color/grey"
            android:textSize="14sp"/>

        <ImageView
            android:id="@+id/book_service_search_voice_iv"
            android:layout_width="10dp"
            android:layout_height="16dp"
            android:layout_alignParentRight="true"
            android:layout_alignRight="@+id/search_edit"
            android:layout_centerVertical="true"
            android:layout_marginRight="6dp"
            android:background="@drawable/btn_yuyin_nor"
            android:visibility="visible"/>
    </RelativeLayout>

</RelativeLayout>